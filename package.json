{
  "name": "odontologia-atendimento",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "dev": "node --env-file-if-exists=.env server/dev.js",
    "build": "node scripts/build.js",
    "test": "node --test tests/*.test.js"
  },
  "devDependencies": {
    "@electric-sql/pglite": "^0.5.8"
  }
}
